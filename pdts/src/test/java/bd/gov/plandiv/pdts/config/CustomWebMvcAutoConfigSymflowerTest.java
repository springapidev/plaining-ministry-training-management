package bd.gov.plandiv.pdts.config;

import org.junit.jupiter.api.*;
import org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry;

public class CustomWebMvcAutoConfigSymflowerTest {
	@Test
	public void addResourceHandlers1() {
		CustomWebMvcAutoConfig c = null; // TODO This is a fallback value due to incomplete analysis.
		ResourceHandlerRegistry registry = null; // TODO This is a fallback value due to incomplete analysis.
		c.addResourceHandlers(registry);
	}
}
